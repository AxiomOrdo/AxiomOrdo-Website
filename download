import { packages } from "./site-content";

export function PricingSection() {
  return (
    <section className="section section-slate" id="packages">
      <div className="container">
        <div className="section-heading">
          <p className="eyebrow">Packages</p>
          <h2>Choose the level of certainty you need</h2>
          <p>
            From initial exposure visibility to full defensibility under
            challenge.
          </p>
        </div>

        <div className="pricing-grid">
          {packages.map((pkg) => (
            <article
              className={pkg.featured ? "price-card featured" : "price-card"}
              key={pkg.name}
            >
              {pkg.badge ? <div className="badge">{pkg.badge}</div> : null}
              <h3>{pkg.name}</h3>
              <p className="price">{pkg.price}</p>
              <p className="card-intro">{pkg.intro}</p>
              <ul className="check-list">
                {pkg.bullets.map((bullet) => (
                  <li key={bullet}>{bullet}</li>
                ))}
              </ul>
              <p className="best-for">
                <strong>Best for:</strong> {pkg.bestFor}
              </p>
              <a className="button button-primary full-width" href="#contact">
                {pkg.cta}
              </a>
            </article>
          ))}
        </div>
      </div>
    </section>
  );
}
